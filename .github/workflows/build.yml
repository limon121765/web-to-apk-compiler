name: Build APK

on:
  repository_dispatch:
    types: [build_apk]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Receive Data
        run: |
          echo "App URL: ${{ github.event.client_payload.url }}"
          echo "Doc ID: ${{ github.event.client_payload.docId }}"
          
      - name: Build APK (Simulation)
        run: |
          echo "Simulating APK Generation..."
          sleep 15
          
      - name: Update Firebase Database
        run: |
          PROJECT_ID="my-app-ae88f"
          DOC_ID="${{ github.event.client_payload.docId }}"
          API_KEY="AIzaSyAo9ddKYbaCiIz5iwCNJN0pxs3UQvBvrTI"
          
          curl -X PATCH "https://firestore.googleapis.com/v1/projects/$PROJECT_ID/databases/(default)/documents/apks/$DOC_ID?key=$API_KEY&updateMask.fieldPaths=status&updateMask.fieldPaths=downloadUrl" \
          -H "Content-Type: application/json" \
          -d '{
            "fields": {
              "status": { "stringValue": "success" },
              "downloadUrl": { "stringValue": "https://example.com/download/app.apk" }
            }
          }'
